[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "libKriging"
authors = [{name = "libKriging team", email = "libkriging@gmail.com"}]
dynamic = ["version", "description"]
